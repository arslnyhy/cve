---
.show_pon_ctlr: &show_pon_ctlr 'show running-config pon-ctlr'

defaults:
  devices:
    - platform: cisco_iosxr

tests:
  rule_cve202420483:
    - outcome: TESTS_FAILED
      commands:
        *show_pon_ctlr: |
          pon-ctlr

    - outcome: OK
      commands:
        *show_pon_ctlr:
          # no pon-ctlr