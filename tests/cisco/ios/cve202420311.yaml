defaults:
  devices:
    - platform: cisco_ios

tests:
  rule_cve202420311:
    - outcome: TEST_FAILED
      commands:
        check_lisp: |
          router lisp

    - outcome: TEST_PASSED
      commands:
        check_lisp: |
          no router lisp
