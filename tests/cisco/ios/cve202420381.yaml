defaults:
  devices:
    - platform: cisco_ios

tests:
  rule_cve202420381:
    - outcome: TEST_FAILED
      commands:
        show_confd_conf: |
          <confd.conf>
            <webui>
              <enabled>true</enabled>
            </webui>
          </confd.conf>

    - outcome: TEST_PASSED
      commands:
        show_confd_conf: |
          <confd.conf>
            <webui>
              <enabled>false</enabled>
            </webui>
          </confd.conf>
