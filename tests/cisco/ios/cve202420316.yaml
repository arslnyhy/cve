defaults:
  devices:
    - platform: cisco_ios

tests:
  rule_cve202420316:
    - outcome: TEST_FAILED
      commands:
        show_netconf: |
          netconf-yang
        show_restconf: |
          restconf
        show_logs: |
          %DMI-5-SYNC_NEEDED: Configuration sync needed
          %DMI-5-SYNC_START: Configuration sync started
          %DMI-3-SYNC_ERR: Configuration sync error
          %DMI-3-DMI_DEGRADED: DMI degraded

    - outcome: TEST_PASSED
      commands:
        show_netconf: |
          no netconf-yang
        show_restconf: |
          no restconf
        show_logs: |
          No relevant DMI logs found
