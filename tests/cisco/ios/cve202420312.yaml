defaults:
  devices:
    - platform: cisco_ios

tests:
  rule_cve202420312:
    - outcome: TEST_FAILED
      commands:
        show_running_config: |
          router isis
           is-type level-1

    - outcome: TEST_PASSED
      commands:
        show_running_config: |
          router isis
           is-type level-2-only
